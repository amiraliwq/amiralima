import { pgTable, serial, text, integer, timestamp, boolean, pgEnum, jsonb } from 'drizzle-orm/pg-core';
export const roleEnum=pgEnum('role',['admin','user']);
export const productTypeEnum=pgEnum('product_type',['course','digital_asset','physical_hardware']);
export const categoryEnum=pgEnum('category',['photoshop','fonts','robotics','programming','electronics']);
export const orderStatusEnum=pgEnum('order_status',['pending_payment','slip_uploaded','approved','rejected']);
export const enrollmentStatusEnum=pgEnum('enrollment_status',['active','revoked']);
export const users=pgTable('users',{id:serial('id').primaryKey(),firstName:text('first_name').notNull(),lastName:text('last_name').notNull(),username:text('username').notNull().unique(),phone:text('phone').notNull(),altPhone:text('alt_phone'),passwordHash:text('password_hash').notNull(),role:roleEnum('role').default('user').notNull(),createdAt:timestamp('created_at').defaultNow().notNull()});
export const products=pgTable('products',{id:serial('id').primaryKey(),title:text('title').notNull(),slug:text('slug').notNull().unique(),description:text('description').notNull(),price:integer('price').notNull(),type:productTypeEnum('type').notNull(),category:categoryEnum('category').notNull(),imageUrl:text('image_url'),digitalAssetUrl:text('digital_asset_url'),stock:integer('stock').default(0).notNull(),relatedKitIds:jsonb('related_kit_ids').default([])});
export const courseLessons=pgTable('course_lessons',{id:serial('id').primaryKey(),productId:integer('product_id').notNull().references(()=>products.id),title:text('title').notNull(),description:text('description'),videoUrl:text('video_url'),unlockDate:timestamp('unlock_date'),sortOrder:integer('sort_order').default(0).notNull()});
export const enrollments=pgTable('enrollments',{id:serial('id').primaryKey(),userId:integer('user_id').notNull().references(()=>users.id),productId:integer('product_id').notNull().references(()=>products.id),status:enrollmentStatusEnum('status').default('active').notNull(),createdAt:timestamp('created_at').defaultNow().notNull()});
export const liveClasses=pgTable('live_classes',{id:serial('id').primaryKey(),productId:integer('product_id').notNull().references(()=>products.id),title:text('title').notNull(),scheduledAt:timestamp('scheduled_at').notNull(),meetingUrl:text('meeting_url').notNull(),isActive:boolean('is_active').default(true).notNull()});
export const assignments=pgTable('assignments',{id:serial('id').primaryKey(),productId:integer('product_id').notNull().references(()=>products.id),title:text('title').notNull(),description:text('description'),deadline:timestamp('deadline')});
export const assignmentSubmissions=pgTable('assignment_submissions',{id:serial('id').primaryKey(),assignmentId:integer('assignment_id').notNull().references(()=>assignments.id),userId:integer('user_id').notNull().references(()=>users.id),fileUrl:text('file_url').notNull(),studentNote:text('student_note'),adminGrade:text('admin_grade'),adminFeedback:text('admin_feedback'),submittedAt:timestamp('submitted_at').defaultNow().notNull()});
export const orders=pgTable('orders',{id:serial('id').primaryKey(),userId:integer('user_id').notNull().references(()=>users.id),totalAmount:integer('total_amount').notNull(),status:orderStatusEnum('status').default('pending_payment').notNull(),createdAt:timestamp('created_at').defaultNow().notNull()});
export const shippingAddresses=pgTable('shipping_addresses',{id:serial('id').primaryKey(),orderId:integer('order_id').notNull().references(()=>orders.id),fullName:text('full_name').notNull(),phone:text('phone').notNull(),postalCode:text('postal_code').notNull(),address:text('address').notNull(),floor:text('floor'),unit:text('unit'),ringBell:text('ring_bell')});
export const paymentReceipts=pgTable('payment_receipts',{id:serial('id').primaryKey(),orderId:integer('order_id').notNull().references(()=>orders.id),receiptImageUrl:text('receipt_image_url').notNull(),aiDetectedAmount:integer('ai_detected_amount'),aiCardMatch:boolean('ai_card_match'),adminApproved:boolean('admin_approved').default(false).notNull(),createdAt:timestamp('created_at').defaultNow().notNull()});
